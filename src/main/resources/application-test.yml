spring:
  http:
    encoding:
      charset: UTF-8
  mvc:
    date-format: yyyy-MM-dd
  jackson:
    date-format: yyyy-MM-dd hh:mm:ss
    time-zone: GMT+8
  thymeleaf:
    cache: false


logging:
  config: classpath:logback_test.xml
